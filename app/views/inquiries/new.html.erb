<div class='topPage'>
  <div class="container">
    <h1>ファッションいいね通知</h1>
    <p>あなたの周りにいる友人のファッションが良いと感じたら気軽に「オシャレだね！」と教えてくれる機能です。
    </p>
    <%# 関連するinquiryモデルがある為、[form_with]を使う %>
    <%= form_with(model: @inquiry, url: inquiries_confirm_path, local: true) do |f| %>
      <div class="form-group">
       <%= f.label :name %>
        <%= f.text_field :name, autofocus: true, class: "form-control", placeholder: "お名前" %>
      </div>
      <div class="form-group">
       <%= f.label :email %>
        <%= f.text_field :email, autofocus: true, class: "form-control", placeholder: "メールアドレス" %>
      </div>
      <div class="form-group">
        <%= f.label :relationship %>
        <%= f.select(:relationship, [["友人", "友人"], ["家族", "家族"], ["恋人", "恋人"],["上司","上司"], ["先輩", "先輩"]], {}, class: "form-control") %>
      </div>
      <div class="form-group">
        <%= f.label :content %>
        <%= f.text_area :content, class: "form-control", placeholder: "例トップスが良いね！等" %>
      </div>
       <div class="text-center"><%= f.submit '確認画面へ', class: 'btn btn-primary' %></div>
    <% end %>
  </div>
</div>
